#! /usr/bin/env python
# coding=utf-8

# built-in color picker on macos only out put digit, hence this

import sys

result = ''

for arg in sys.argv[1:]:
    hx = hex(int(arg))
    result += hx.replace("0x", '')

print(result)
